---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addons-kf-jupyter-web-app
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - merge:
        mergeKeys: [server]
        generators:
          - clusters:
              values:
                namespace: kubeflow
          - clusters:
              selector:
                matchLabels:
                  environment: staging
              values: {}
          - clusters:
              selector:
                matchLabels:
                  environment: prod
              values: {}
  template:
    metadata:
      name: addon-{{name}}-kf-jupyter-web-app
      annotations:
        argocd.argoproj.io/sync-wave: "100"
    spec:
      project: default
      source:
        path: apps/jupyter/jupyter-web-app/upstream/overlays/istio
        repoURL: https://github.com/kubeflow/manifests
        targetRevision: 776d4f4
      destination:
        namespace: '{{values.namespace}}'
        name: '{{name}}'
      syncPolicy:
        automated:
          prune: false
        syncOptions:
          - ServerSideApply=true  # Big CRDs.
